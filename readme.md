# Setup Tutorial

## Docker setup

```bash
  sudo docker run -p 2881:2881 --name obstandalone -e MODE=MINI -d quay.io/oceanbase/oceanbase-ce
```

## Finally

Run this project (Specify the IP, user, password and Database name if needed).